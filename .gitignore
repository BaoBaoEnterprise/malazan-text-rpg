# Python
__pycache__/
*.py[cod]
*$py.class

# Claude Code personal overrides
.claude/settings.local.json

# Server dev environment
.venv/

# Live game state (world.db, saves, telemetry) — never committed
world/*
!world/.gitkeep

# Lore sources — PRIVATE, must never reach the public repo (plan §5).
# Guard is in place before any book file ever lands on disk.
lore/books/
lore/raw/
lore/corpus.db
